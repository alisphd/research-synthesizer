# No secret build-time environment variables are required for GitHub Pages.
# The app asks each user for a Gemini API key at runtime and stores it locally.

# Optional: set this when building for a GitHub project site.
# Example: /research-synthesizer/
VITE_BASE_PATH="/research-synthesizer/"
